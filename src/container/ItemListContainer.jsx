import React from "react";
import { Container } from "react-bootstrap";

const ItemListContainer = ({basic}) => {
    return(
    <Container>
        <h1> {basic} </h1>
    </Container>
    )
}

export default ItemListContainer